interface:
  display_name: "OpenPI π0.5 Learning Coach"
  short_description: "Trace π0.5 architecture, training, inference, and evidence."
  default_prompt: "Use $openpi-pi05-learning-coach to trace the current OpenPI π0.5 path from inputs and language conditioning through Flow Matching actions."
