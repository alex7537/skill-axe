interface:
  display_name: "TI-ONE"
  short_description: "Inspect and plan TI-ONE tasks, services, and alerts"
  default_prompt: "Use $tione to inspect my running TI-ONE training tasks."

policy:
  allow_implicit_invocation: true
